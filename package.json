{
  "name": "pi-fast-jev-compaction",
  "version": "0.7.4",
  "description": "Jev pruning for Pi with ceiling-relative hysteresis, a pruning cost gate, and bounded task-state excerpts.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "jev",
    "compaction",
    "context"
  ],
  "engines": {
    "node": ">=22"
  },
  "pi": {
    "extensions": [
      "./extensions/fast-jev.ts"
    ]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  }
}
