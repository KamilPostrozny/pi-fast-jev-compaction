{
  "name": "pi-fast-jev-compaction",
  "version": "0.6.3",
  "description": "Restored 0.5 Jev scheduling behavior for Pi with explicit result pruning and calibrated native fallback.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "jev",
    "compaction",
    "context"
  ],
  "engines": {
    "node": ">=22"
  },
  "pi": {
    "extensions": [
      "./extensions/fast-jev.ts"
    ]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  }
}
