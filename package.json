{
  "name": "pi-fast-jev-compaction",
  "version": "0.7.2",
  "description": "Jev pruning for Pi with real-usage pressure episodes and 70% re-arm hysteresis.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "jev",
    "compaction",
    "context"
  ],
  "engines": {
    "node": ">=22"
  },
  "pi": {
    "extensions": [
      "./extensions/fast-jev.ts"
    ]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  }
}
